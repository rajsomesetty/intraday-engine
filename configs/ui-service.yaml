apiVersion: v1
kind: Service
metadata:
  name: intraday-ui-service
  namespace: intraday

spec:
  type: NodePort

  selector:
    app: intraday-ui

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008
